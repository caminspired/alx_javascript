#!/usr/bin/node
class Rectangle {
    constructor(w, h) {
        if (width <= 0 || isNaN(width) || height <= 0 || isNaN(height)) {};    
        width = w;
        height = h;  
    }
}



module.exports = Rectangle;
